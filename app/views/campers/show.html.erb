<div class="back-list mt-5">
  <a href="/campers/">⬅️ back to campers</a>
</div>
<div class="d-flex justify-content-center align-items-center">
  <% if @camper.image.attached? %>
  <div class="image-show" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3))", url('<%= cl_image_path @camper.image.key, height: 300, width: 200, crop: :fill %>')">
  <% else %>
  <%= image_tag("camper.jpg", style: "background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3));", class: "image-show") %>
  <% end %>
  </div>
</div>
<div class="card-information">
  <div>
    <h2><%= @camper.name %></h2>
    <h4><%= @camper.description %></h4>
  </div>
  <div class="price-booking-text">
    <h4><%= @camper.price %>€ / day</h4>
    <%= link_to "Book now!", new_camper_booking_path(@camper), class: "btn btn-warning mt-3", style: "border-radius: 6px;" "color: white;" %>
    <div>
      <% if policy(@camper).edit? %>
      <%= link_to "Edit your camper", edit_camper_path, class: "btn btn-secondary mt-3", style: "border-radius: 6px;" "color: white;" %>
      <% end %>
    </div>
  </div>
</div>
<div class="mb-5" id="map"
     style="width: 60%; margin:0 auto; height: 600px; border-radius: 30px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
